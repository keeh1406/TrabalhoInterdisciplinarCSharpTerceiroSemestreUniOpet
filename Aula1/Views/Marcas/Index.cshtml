@model IEnumerable<Aula1.Models.Marca>

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Lista de Marcas";
}

<div class="panel	panel-primary">
    <div class="panel-heading">
        Relação	de Marcas Registrados
    </div>
    <div class="panel-body">
        <table class="table	table-striped	table-hover">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.MarcaId)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Name)
                    </th>
                    <th>Ações</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.MarcaId)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Name)
                        </td>
                        <td>
                            @Html.ActionLink("Editar", "Edit", new { id = item.MarcaId })	|
                            @Html.ActionLink("Detalhes", "Details", new { id = item.MarcaId })	|
                            @Html.ActionLink("Deletar", "Delete", new { id = item.MarcaId })
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
    <div class="panel-footer	panel-info">
        @Html.ActionLink("Criar nova marca", "Create", null, new { @class = "btn	btn-success" })
    </div>
</div>
@section	styles{<link href="@Url.Content("~/content/DataTables/css/dataTables.bootstrap.css")" rel="stylesheet" />}
@section	ScriptPage	{
    <script src=" @Url.Content("~/scripts/DataTables/jquery.dataTables.js")"></script>
    <script src="@Url.Content("~/scripts/DataTables/dataTables.bootstrap.js")"></script>
    <script type="text/javascript">
        $(document).ready(function () {
            $(document).ready(function () {
                $('li').removeClass("active");
                $('#liMarcas').addClass("active");
                $('.table').dataTable({ "order": [[1, "asc"]] });
            });
    </script>
}
